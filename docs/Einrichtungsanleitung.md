### starten mit Funktionen bis Abschnitt 6
- '/src/' auf raspi kopieren
- gmu ausführen
    ```bash
    python3 gmu.py --loop --segment --lcd --matrix --hum --temp --light --csv --relay -u 4
    ```
